[server]
http_port = 3000

[security]
admin_user = admin
admin_password = admin

[auth.anonymous]
enabled = false

[dashboards]
default_home_dashboard_path = /var/lib/grafana/dashboards/finGuard-dashboard.json

[datasources]
path = /etc/grafana/provisioning/datasources
